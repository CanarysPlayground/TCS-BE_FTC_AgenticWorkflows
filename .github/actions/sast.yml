name: SAST Scan
#sast
runs:
  using: composite

  steps:
    - name: Install Semgrep
      shell: bash
      run: |
        python3 -m pip install semgrep

    - name: Run Semgrep Scan
      shell: bash
      run: |
        semgrep --config=auto .
